{
  "required": "Se requiere campo",
  "whitespace": "${label} no puede estar vacío",
  "format": "${label} no es válido para la fecha de formato",
  "parse": "${label} no se pudo analizar como fecha",
  "invalid": "${label} es fecha inválida",
  "type": "${label} no es válido ${type}",
  "string.len": "${label} debe ser exactamente ${len} caracteres",
  "string.min": "${label} al menos debe ser ${min} caracteres",
  "string.max": "${label} no puede ser más largo que ${max} caracteres",
  "string.range": "${label} debe estar entre ${min} y ${max} caracteres",
  "number.len": "${label} debe ser igual ${len}",
  "number.min": "${label} no puede ser menor que ${min}",
  "number.max": "${label} no puede ser mayor que ${max}",
  "number.range": "${label} debe estar entre ${min} y ${max}",
  "array.len": "${label} debe ser exactamente ${len} en longitud",
  "array.min": "${label} no puede ser menor que ${min} en longitud",
  "array.max": "${label} no puede ser mayor que ${max} en longitud",
  "array.range": "${label} debe estar entre ${min} y ${max} en longitud",
  "pattern.mismatch": "${label} no coincide con el patrón ${pattern}",
  "notaphonenumber": "No es un numero de telefono",
  "notavalidphonenumber": "No es un número de teléfono válido.",
  "addresshistory": "Menos de 3 años. Por favor mencione las direcciones anteriores.",
  "notvalidssn": "No es un ssn válido",
  "wronglicenseformat": "¡Formato erróneo!"
}
