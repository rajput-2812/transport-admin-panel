{
  "required": "Field is required",
  "whitespace": "${label} cannot be empty",
  "format": "${label} is invalid for format date",
  "parse": "${label} could not be parsed as date",
  "invalid": "${label} is invalid date",
  "type": "${label} is not a valid ${type}",
  "string.len": "${label} must be exactly ${len} characters",
  "string.min": "${label} must be at least ${min} characters",
  "string.max": "${label} cannot be longer than ${max} characters",
  "string.range": "${label} must be between ${min} and ${max} characters",
  "number.len": "${label} must equal ${len}",
  "number.min": "${label} cannot be less than ${min}",
  "number.max": "${label} cannot be greater than ${max}",
  "number.range": "${label} must be between ${min} and ${max}",
  "array.len": "${label} must be exactly ${len} in length",
  "array.min": "${label} cannot be less than ${min} in length",
  "array.max": "${label} cannot be greater than ${max} in length",
  "array.range": "${label} must be between ${min} and ${max} in length",
  "pattern.mismatch": "${label} does not match pattern ${pattern}",
  "addresshistory": "Less than 3 years. Please mention previous home addresses.",
  "notaphonenumber": "Not a phone number",
  "notavalidphonenumber": "Not a valid phone number",
  "notvalidssn": "Not a valid ssn",
  "wronglicenseformat": "Wrong format!"
}
